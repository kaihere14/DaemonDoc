/**
 * One vertical rhythm for every landing section.
 *
 * Sections stack their padding, so the gap between two adjacent sections is
 * twice these values: 112px on mobile, 160px from lg up. Before this existed
 * the gaps ran anywhere from 80px to 208px depending on which two sections
 * happened to be neighbours.
 */
export const SECTION_Y = "py-14 lg:py-20";

/** The container every section shares, so all content sits on one left edge. */
export const SECTION_X = "mx-auto max-w-7xl px-4 sm:px-6 lg:px-8";
